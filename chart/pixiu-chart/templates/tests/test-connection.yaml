apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pixiu.fullname" . }}-test-connection"
  labels:
    {{- include "pixiu.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pixiu.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
