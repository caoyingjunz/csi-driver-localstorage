name: Client-gen

on:
  pull_request:
    branches:
      - master

env:
  GO_VERSION: '1.17.3'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: client-gen
        run: make client-gen
